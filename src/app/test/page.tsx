import Editor from "@/src/components/TextEditor/Editor";
import React from "react";

const page = () => {
  return (
    <div className="relative">
      <Editor />
    </div>
  );
};

export default page;
